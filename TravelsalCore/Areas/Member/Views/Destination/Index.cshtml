@using EntityLayer.Concrete
@model List<Destination>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

@*<h1>Aktif Tur Rotalarımız</h1>
<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Şehir</th>
        <th>Süre</th>
        <th>Kapasite</th>
        <th>Fiyat</th>
        <th>Detaylar</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.DestinationID</td>
            <td>@item.City</td>
            <td>@item.DayNight</td>
            <td>@item.Capacity</td>
            <td>$@item.Price</td>
            <td><a href="#" class="btn btn-outline-info">Detaylar</a></td>
        </tr>
    }
</table>

*@
<div class="row">
    <div class="col-12">
        <div class="card mb-4">
            <div class="card-header pb-0">
                <h6>Aktif Tur Rotalarımız</h6>
            </div>
            <div class="card-body px-0 pt-0 pb-2">
                <div class="table-responsive p-0">
                    <table class="table align-items-center mb-0">
                            <tr>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">#</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Şehir</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Süre</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Kapasite</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Fiyat</th>
                                <th class="text-uppercase text-secondary text-xxs font-weight-bolder opacity-7">Detaylar</th>
                            </tr>
                            @foreach (var item in Model)
                            {
                             <tr>                                
                                    <td>
                                         <p class="text-xs font-weight-bold mb-0">@item.DestinationID</p>
                                    </td>
                                    <td>
                                        <p class="text-xs font-weight-bold mb-0">@item.City</p>
                                    </td>
                                    <td>
                                        <p class="text-xs font-weight-bold mb-0">@item.DayNight</p>
                                    </td>
                                    <td>
                                        <p class="text-xs font-weight-bold mb-0">@item.Capacity</p>
                                    </td>
                                    <td>
                                        <p class="text-xs font-weight-bold mb-0">@item.Price</p>
                                    </td>
                                <td>
                                 <span class="badge badge-sm bg-gradient-info"><a href="#">Detaylar</a></span>
                                </td>
                            </tr>
                            }

                    </table>
                </div>
            </div>
        </div>
    </div>
</div>